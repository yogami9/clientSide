# Banking Client Configuration

# Connection Type (REST or RMI)
connection.type=REST

# REST API Configuration
rest.api.url=http://localhost:8080

# RMI Configuration
rmi.host=localhost
rmi.port=1099
